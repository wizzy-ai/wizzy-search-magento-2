<?php
    $configs = $block->getConfigs();
    $alignment = $configs['autocomplete']['menu']['alignment'];
    $noResultsText = $configs['autocomplete']['menu']['noResultsText'];
?>

<script type="text/template" id="wizzy-autocomplete-wrapper">
    <div class="wizzy-autocomplete-wrapper
    <?php if ($alignment == "right") { ?> onRight <?php } else { ?> onLeft <?php } ?>
    {{^topProducts}}withoutTopProducts{{/topProducts}} {{^hasSuggestions}}withoutSuggestions{{/hasSuggestions}}
    {{#hasToShowBanner}} withPromotionBanners  {{/hasToShowBanner}}"
         style="display: {{^isMenuHidden}}flex{{/isMenuHidden}}">
    {{#hasToShowBannerAtTop}} {{&singleBanner}}{{/hasToShowBannerAtTop}}
    <div class="wz-autocomplete-sugegstions-products-wrapper">
        {{#topProducts}}
            {{{topProducts}}}
        {{/topProducts}}
        {{#suggestions}}
            {{{suggestions}}}
        {{/suggestions}}
        {{^suggestions}}
            {{^topProducts}}
                <div class="autocomplete-no-results">
                    <p><?= $block->escapeHtmlAttr($noResultsText); ?></p>
                </div>
            {{/topProducts}}
        {{/suggestions}}
    </div>
    {{#hasToShowBannerAtBottom}} {{&singleBanner}}{{/hasToShowBannerAtBottom}}
    <!-- ending of wizzy-autocomplete-wrapper -->
</script>